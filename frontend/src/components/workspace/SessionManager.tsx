export default function SessionManager() {
  
  // SessionManager is no longer needed since sessions are managed per tab
  // Sessions start automatically when files are validated and persist until tab is closed
  return null;
}